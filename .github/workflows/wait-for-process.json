{
  "version": "1.0",
  "description": "waits for the tcp client to connect, if no connection in 360 secs, then it fails.",
  "tcpPort": 6215,
  "connectionTimeout": 360,
  "testedApps": [
    {
      "name": "Test runner app",
      "commands": [
        "{wait-for-process}"
      ]
    }
  ]
}